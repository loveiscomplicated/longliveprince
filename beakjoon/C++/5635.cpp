#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int main() {
  int num_case;
  cin >> num_case;
  
  string name_arr[num_case];
  int day_arr[num_case];
  int month_arr[num_case];
  int year_arr[num_case];

  for (int i = 0; i < num_case; i++) {
    double remain = i % 4;
    if (remain == 0) {
      
    }
  }

  return 0;
}